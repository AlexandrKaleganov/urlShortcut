import { RolesNamePipe } from './roles-name.pipe';

describe('RolesNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RolesNamePipe();
    expect(pipe).toBeTruthy();
  });
});
